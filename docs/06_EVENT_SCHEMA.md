# Event Schema (MVP)

Events are the base unit of AIR4 memory.

Minimal schema:

id
timestamp
original_text
processed_text
metadata (JSON)
embedding

Users only write text; AIR4 extracts structure automatically.
